<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/ct_purple</item>
        <item name="colorPrimaryDark">@color/ct_black</item>
        <item name="colorAccent">@color/ct_light_grey</item>
        <item name="android:windowBackground">@drawable/background</item>
    </style>

    <style name="starRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/custom_ratingbar_full</item>
        <item name="android:minHeight">5dp</item>
        <item name="android:maxHeight">100dp</item>
        <item name="android:adjustViewBounds">true</item>
        <item name="android:thumb">@null</item>
    </style>

    <style name="starRatingBar2" parent="@android:style/Widget.RatingBar">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/custom_ratingbar_full2</item>
        <item name="android:minHeight">15dp</item>
        <item name="android:maxHeight">100dp</item>
        <item name="android:adjustViewBounds">true</item>
        <item name="android:thumb">@null</item>
    </style>

</resources>